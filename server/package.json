{
  "name": "ls",
  "version": "0.0.0",
  "description": "example node api server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "db:migrate": "node -e 'require(\"./db/migrations\").runMigrations()'"
  },
  "author": "toglander@1904labs.com",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mssql": "^9.1.1",
    "pg": "^8.10.0"
  }
}
